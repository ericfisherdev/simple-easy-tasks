# Development Environment Configuration
# This file is safe to commit as it contains only development defaults

# Server Configuration
SERVER_PORT=8080
GIN_MODE=debug
LOG_LEVEL=debug
ENVIRONMENT=development

# Database Configuration
DATABASE_URL=http://localhost:8090
PB_PORT=8090
PB_ADMIN_EMAIL=admin@localhost
PB_ADMIN_PASSWORD=development123

# Redis Configuration
REDIS_URL=redis://localhost:6379
REDIS_PORT=6379
REDIS_PASSWORD=

# Security Configuration (Development only - generate new for production!)
JWT_SECRET=simple-easy-tasks-development-jwt-secret-key-32chars-minimum-length-required
JWT_EXPIRATION=24h
REFRESH_TOKEN_EXPIRATION=168h

# CORS Configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000,http://127.0.0.1:8080

# Docker Configuration
APP_PORT=8080
NGINX_PORT=80
NGINX_HTTPS_PORT=443

# Build Arguments
VERSION=dev
BUILD_TIME=
COMMIT_HASH=

# Development-specific timeouts (more relaxed)
READ_TIMEOUT=30s
WRITE_TIMEOUT=30s
IDLE_TIMEOUT=120s
CONNECTION_TIMEOUT=60s

# Database Connection Pool
MAX_CONNECTIONS=10

# Logging
LOG_FORMAT=text
LOG_OUTPUT=stdout